# Removal of `plot_usage_area_by_individual`

## Objective

`plot_usage_area_by_individual()` has been fully removed: function deleted from `R/cli.R`, test case removed from `tests/testthat/test_cli.R`, and changelog updated. `NAMESPACE` and `man/*.Rd` are auto-generated by roxygen2 — no manual cleanup needed.

## Key File

- `tests/testthat/test_cli.R`: Contains the test case for the function.

## Implementation Steps

- [x] Remove `plot_usage_area_by_individual` from `R/cli.R`.
- [x] Remove the test case in `tests/testthat/test_cli.R`. 
- [x] Add a `### Removed` entry under `## [Unreleased]` in `CHANGELOG.md` announcing the removal.

## Verification & Testing

- [x] Run existing tests to ensure no regressions: `make tests`.
- [x] Confirm `plot_usage_area_by_individual` is no longer available in the package.
- [x] Run code coverage tools to verify the test case is removed and coverage is accurate.
