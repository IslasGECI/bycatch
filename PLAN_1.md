# Removal of `plot_usage_area_by_individual`

## Objective
Remove `plot_usage_area_by_individual()` and its test case from the codebase. The function definition is gone; `NAMESPACE` and `man/*.Rd` are auto-generated by roxygen2 — no manual cleanup needed.

## Key File
- `tests/testthat/test_cli.R`: Contains the test case for the function.

## Implementation Steps
1. ~~Remove `plot_usage_area_by_individual` from `R/cli.R`.~~ **Done.**
2. Add a `### Removed` entry under `## [Unreleased]` in `CHANGELOG.md` announcing the removal.
3. Remove the test case in `tests/testthat/test_cli.R`. *(pending)*

## Verification & Testing
- Run existing tests to ensure no regressions: `make tests`.
- Confirm `plot_usage_area_by_individual` is no longer available in the package.
- Run code coverage tools to verify the test case is removed and coverage is accurate.
